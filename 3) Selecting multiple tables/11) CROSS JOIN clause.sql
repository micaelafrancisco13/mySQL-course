-- CROSS JOIN is used to combine every possibility of two sets
-- (e.g., colors × sizes = product variations)

-- IMPORTANT: as a real-world example, CROSS JOIN is used
-- to combine every color with every size
-- (e.g., red-xs, red-s, red-m, red-l, red-xl)

-- implicit syntax:
-- 		FROM Table1, Table2
-- explicit syntax:
-- 		FROM Table1
-- 		CROSS JOIN Table 2
